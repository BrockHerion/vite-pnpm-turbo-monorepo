import { User } from "@demo/shared/types";

export default function UserDisplay() {
  const user: User = {
    firstName: "Brock",
    lastName: "Herion",
    isAdmin: true,
    username: "brock.herion",
  };

  return <></>;
}
